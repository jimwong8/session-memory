global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/session-memory-alerts.yml

scrape_configs:
  - job_name: 'session-memory-api'
    static_configs:
      - targets: ['api:8000']
    metrics_path: '/metrics'
